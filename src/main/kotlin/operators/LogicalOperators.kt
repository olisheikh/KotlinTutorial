package operators

fun main() {
    // Two operands
    var a = 10
    var b = 20

    // And operator(&&)
    println("And operator: ${a > b && a < b}")

    // Or operator(||)
    println("Or operator: ${a > b || a < b}")

    // Not operator(!)
    println("Not operator: ${!true}")

}